# Les probabilités et le dénombrement dans le poker

## Prérequis
- Python 3.12 ou version supérieure
- Git (facultatif)

## Installation
1. Cloner le dépôt :  
```bash
git clone https://github.com/zerofish0/poker_simulation.git
```

ou télécharger directement le dépôt depuis GitHub.

2. Se placer dans le répertoire du projet :

```bash
cd poker_simulation
```

3. Installer les dépendances :

```bash
pip install -r requirements.txt
```

## Lancement

```bash
python3 main.py
```

## Fonctionnement interne

Le programme parcourt toutes les mains possibles (`ALL_POSSIBLE_HANDS`) et teste si chaque main valide une combinaison spécifique (paire, double paire, brelan, quinte, flush, full, carré, straight flush, royal flush, hauteur). Les résultats sont ensuite comptabilisés pour chaque catégorie.



# English version

# Probabilities and Counting in Poker

## Requirements
- Python 3.12 or higher
- Git (optional)

## Installation
1. Clone the repository:  
```bash
git clone https://github.com/zerofish0/poker_simulation.git
``
or download the repository directly from GitHub.

2. Navigate to the project directory:

```bash
cd poker_simulation
```

3. Install the dependencies:

```bash
pip install -r requirements.txt
```

## Running the program

```bash
python3 main.py
```

## Internal workings
The program goes through all possible hands (ALL_POSSIBLE_HANDS) and checks whether each hand satisfies a specific combination (one pair, two pairs, three of a kind, straight, flush, full house, four of a kind, straight flush, royal flush, high card). The results are then counted for each category.

# Deutsch version

# Wahrscheinlichkeiten und Zählung im Poker

## Voraussetzungen
- Python 3.12 oder höher
- Git (optional)

## Installation
1. Repository klonen: 
```bash
git clone https://github.com/zerofish0/poker_simulation.git
``
oder das Repository direkt von GitHub herunterladen.

2. In das Projektverzeichnis wechseln:

```bash
cd poker_simulation
```

3. Abhängigkeiten installieren:

```bash
pip install -r requirements.txt
```

## Programm starten

```bash
python3 main.py
```

## Interne Funktionsweise
Das Programm geht alle möglichen Hände (ALL_POSSIBLE_HANDS) durch und überprüft, ob jede Hand eine bestimmte Kombination erfüllt (ein Paar, zwei Paare, Drilling, Straße, Flush, Full House, Vierling, Straight Flush, Royal Flush, High Card). Die Ergebnisse werden anschließend für jede Kategorie gezählt.

